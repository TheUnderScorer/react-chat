/**
 * Handles create-chat endpoint
 *
 * @file create-chat.js
 *
 * */

const JsonResponse = require( '../../helpers/jsonResponse' ),
	  User         = require( '../../db/models/User' ),
	  Validator    = require( '../../helpers/Validator' );

module.exports = async ( req, res ) => {

};